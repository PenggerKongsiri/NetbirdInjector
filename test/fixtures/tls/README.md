# Test certificates

These are fake, public test-only credentials generated for `upstream.test` and `127.0.0.1`. They protect no real system and must never be reused outside the automated local test environment.
